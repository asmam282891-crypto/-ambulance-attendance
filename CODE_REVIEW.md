# تقرير مراجعة الكود

## ما تم إصلاحه

### 1. منع فشل التشغيل بسبب إعدادات Supabase

كان التطبيق يبدأ بقيم placeholder، لذلك كان يفشل عند تشغيله قبل أن يتمكن
المستخدم من تسجيل الدخول. أصبحت القيم تُمرّر وقت التشغيل عبر:

```bash
--dart-define=SUPABASE_URL=...
--dart-define=SUPABASE_ANON_KEY=...
```

### 2. حماية سجلات الحضور من الكتابة المباشرة

كان العميل يستطيع، نظريًا، إرسال `employee_id` أو أوقات مصطنعة أو تعديل
سجل حضور قائم مباشرة عبر PostgREST. كما كان فحص التسجيل المفتوح منفصلًا عن
عملية الإدخال، مما يسمح بتسجيلين متزامنين.

تم نقل الحضور والانصراف إلى دالتي RPC ذريتين:

- `check_in(...)`
- `check_out(...)`

وأضيف قيد unique جزئي يمنع أكثر من سجل مفتوح للموظف نفسه، مع سحب صلاحيات
`insert/update/delete` المباشرة من أدوار العميل.

### 3. تقييد إحصائيات لوحة المدير

كانت `dashboard_stats()` دالة `security definer` بلا تحقق من دور المستدعي،
وبلا `search_path` ثابت. أصبحت تتحقق من كون المستخدم مديرًا، وتستخدم
`set search_path = public`، ولا تُمنح إلا للمستخدمين المسجلين.

### 4. إصلاح مشاكل واجهة الاستخدام والطلبات غير المتزامنة

- منع `setState` بعد التخلص من الشاشة أثناء تحديد الموقع أو الحضور.
- منع تعطل قارئ QR عند وصول نتيجة بلا barcodes.
- إضافة debounce للبحث عن الموظفين ومنع استجابة قديمة من الكتابة فوق نتيجة
  بحث أحدث.
- قبول قيم الإحصائيات سواء أعادها Supabase كـ `int` أو `double`.
- توحيد رسالة خطأ بيانات الدخول بالعربية.

## نقاط مهمة ما زالت تحتاج قرارًا أو إعدادًا

- يجب وضع إحداثيات المركز الفعلية بدل القيم التجريبية في
  `lib/services/location_service.dart`.
- التحقق من الموقع حاليًا على الجهاز؛ إذا كان الحضور يحتاج حماية قوية، يجب
  نقل geofence إلى RPC/خدمة خادم حتى لا تكون إحداثيات العميل وحدها مصدر
  الثقة.
- قيمة QR تُحفظ فقط ولا تُتحقق من كونها كود نقطة الحضور. يلزم تحديد صيغة
  الكود أو سرّ المركز قبل إضافة تحقق خادمي.
- زرا **إضافة موظف** و**عرض التقارير** ما زالا يعرضان واجهة فقط بلا شاشات
  أو عمليات مرتبطة.
- الأرشيف الأصلي يحتوي على كود `lib` وملفات الإعداد فقط، ولا يحتوي مجلدي
  Android وiOS؛ لذلك يجب إنشاء منصات Flutter أو إضافتها قبل تشغيل الكاميرا
  والموقع على الهاتف، ثم إضافة صلاحيات النظام المطلوبة.
- لا توجد بيئة Flutter/Dart في مساحة المراجعة الحالية، لذلك تم إجراء تحقق
  ساكن ومراجعة للملفات، ولم يتم تشغيل `flutter analyze` أو بناء APK.

## تشغيل النسخة المعدلة

تم تطبيق `supabase_attendance_migration.sql` فعليًا على مشروع
`ambulace_app` ذي الرابط:
`https://vdqsdoyqpxuiiznaruuj.supabase.co`.

1. migration المطبق يربط التطبيق بجداول `attendance_users` و
   `attendance_records` و`attendance_settings`.
2. أنشئ المستخدمين في Supabase Auth واربطهم بصفوف `profiles` و
   `attendance_users`. في القاعدة الحالية تم اعتماد `control.ambulance`
   كمدير لنظام الحضور.
3. حدّث صلاحيات Android/iOS عند إنشاء المنصات.
4. شغّل:

```bash
flutter pub get
flutter run \
  --dart-define=SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co \
  --dart-define=SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
```

تم حفظ `SUPABASE_ANON_KEY` في Replit Secrets، ولا ينبغي وضعه في Git أو
إرساله داخل المحادثة. استخدم `./run_with_supabase.sh` لتمريره إلى Flutter.